const first = 'Rezvi';
const last = 'Ahmed';

// const fullName = first + last;
const fullName = first + ' ' + last;
// console.log(fullName);


const fullName2 = first.concat(' ').concat(last);
console.log(fullName2);


//incluids

console.log(last.includes('y'))
